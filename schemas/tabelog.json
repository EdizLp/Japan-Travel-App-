{
    "type": "OBJECT",
    "properties": {
        "reservations_availability": {
            "type": "STRING", 
            "enum": ["Yes", "No", "N/A"],
            "description": "Whether the restaurant accepts reservations."
        },
        "reservation_info": {
            "type": "STRING",
            "description": "Additional booking details (e.g., 'Phone only', 'Online only') in English. Use 'N/A' if empty."
        },
        "opening_hours": {
            "type": "ARRAY",
            "items": {
                "type": "OBJECT",
                "properties": {
                    "days": {"type": "ARRAY", "items": {"type": "STRING"}},
                    "time_slot": {"type": "STRING", 
                    "description": "Use 24-hour format 'HH:MM - HH:MM'. If the restaurant is closed on these days, strictly use the word 'Closed'."}
                }
            },
            "description": "List of day/time slots. If no days are listed, assume Mon-Sun."
        },
        "operational_info": {
            "type": "STRING",
            "description": "Only use this to describe the specific week-based rules (e.g., 1st Monday) that the 'days' array can't show. in English. Use 'N/A' if empty."
        }
    },
    "required": ["reservations_availability", "reservation_info", "opening_hours", "operational_info"]
}